using UnityEngine;

public class Trap : MonoBehaviour
{
    public bool isBarrer;
    public int barrerHealth;
    
    
    
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
